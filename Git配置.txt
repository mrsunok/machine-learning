ssh-keygen -t rsa -C"your_email@youremail.com"
ssh-keygen -t rsa -C "mrsun_ok@163.com"

$ git config --global user.name "mrsun"
$ git config --global user.email "mrsun_ok@163.com"
我的密码：mrsun_ok

查询自己信息：git config --list 




vscode中添加git
计算机 > 属性（右击）> 高级系统设置 > 环境变量 >  系统变量下的Path > 编辑 ， 然后新建添加你的Git下的cmd路径和bin路径，然后确定即可。
如：D:\Program Files\Git\cmd
步骤三：

打开vscode，然后 文件 > 首选项 > 设置 ，然后添加 "git.path"键，值为Git目录下的cmd下的git.exe文件。
如： "git.path":"D:/Program Files/Git/cmd/git.exe"
步骤四：



利用git bash提交代码
网页登陆github，新建仓库，建成之后下面自动会提示你怎么操作